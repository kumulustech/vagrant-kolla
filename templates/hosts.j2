127.0.0.1	localhost

# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters

# Hosts
{% for host in groups['all'] %}
{{hostvars[host]['ansible_enp0s8']['ipv4']['address']}} {% if domain is defined %}{{ host }}.{{domain}}{% endif %} {{host}}.local.lan {{host}}
{% endfor %}
